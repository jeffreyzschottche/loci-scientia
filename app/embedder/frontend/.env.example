# Niet nodig voor de device-deployment: nuxt.config.ts defaults naar
# /embedder/api/v1 (same-origin via FastAPI). Alleen overrulen bij externe
# ontwikkeling tegen een aparte Laravel-instance.
# NUXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
